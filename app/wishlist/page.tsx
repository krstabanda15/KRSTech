"use client";import Link from"next/link";import{Heart}from"lucide-react";import{useStore}from"@/components/store";import{products}from"@/data/products";import{ProductCard}from"@/components/product-card";
export default function Wishlist(){const{wishlist}=useStore();const list=products.filter(x=>wishlist.includes(x.id));return <div className="container min-h-[60vh] py-12"><p className="eyebrow">Saved for later</p><h1 className="mt-2 text-4xl font-black">Your wishlist</h1>{list.length?<div className="grid-products mt-8">{list.map(p=><ProductCard key={p.id} p={p}/>)}</div>:<div className="panel mt-8 py-20 text-center"><Heart className="mx-auto text-gray-600" size={48}/><h2 className="mt-5 text-2xl font-bold">Nothing saved yet</h2><p className="muted mt-2">Tap the heart on gear you want to remember.</p><Link href="/shop" className="btn primary mt-6">Browse gear</Link></div>}</div>}
